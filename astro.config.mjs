import { defineConfig } from 'astro/config';

// For a GitHub user site, name the repository: Mid112.github.io
// The site will deploy at: https://mid112.github.io
export default defineConfig({
  site: 'https://mid112.github.io',
  output: 'static'
});
